Keywords that the client and server can use to communicate, we just have to compare the string we receive to determine what we send, subject to change


START
Server sends START to start a round


WAIT
Server sends to client to indicate that game has not started


CONNECT
Client sends CONNECT and server responds with a magic string to ensure we don't connect to a random server


SET_NAME{NAME}
Client sends a username to the server to see if it's available, if available continue if not we send NAME_UNAVAILABLE


NAME_UNAVAILABLE
Server sends to client if username is already taken, client must prompt the user again for another username


CLIENT_PACKET{NAME, TIME_DELTA, TARGET_WORD, WORD_SUBMITTED}
Client sends this to the server every time a player submits a word, NAME is the username, TIME_DELTA is the difference between when the player receives the prompt and when they submit,
TARGET_WORD is the word that the player is supposed to type
WORD_SUBMITTED is the word the player actually typed


FF
Client sends to server if player drops out of the game


SERVER_PACKET{WORD(s)}
The server either sends a list of words or one word at a time, we'll decide after we set the rules


GET_LEADERBOARD
Client sends to the server to get the leaderboard after the game ends


